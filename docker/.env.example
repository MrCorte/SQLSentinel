# docker/.env — copy to docker/.env and fill in real values
# This file is NOT committed. docker/.env is gitignored.
#
# Usage:
#   cp docker/.env.example docker/.env
#   # edit docker/.env with your passwords
#   docker compose up -d

SA_PASSWORD_PROD=changeme-prod
SA_PASSWORD_DEV=changeme-dev
SA_PASSWORD_STAGING=changeme-staging
SA_PASSWORD_DR=changeme-dr
SA_PASSWORD_SENTINEL=changeme-sentinel
